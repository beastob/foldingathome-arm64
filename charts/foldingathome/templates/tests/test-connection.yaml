apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foldingathome.fullname" . }}-test-connection"
  labels:
    {{- include "foldingathome.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foldingathome.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
